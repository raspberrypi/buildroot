[all]
kernel=kernel8.img
initramfs rootfs.cpio.zst
enable_uart=1
uart_2ndstage=1
disable_overscan=1
dtoverlay=dwc2,dr_mode=peripheral
# Set GPU mem to < 32 for cutdown firmware
gpu_mem=16

[pi5]
# Limit clock frequencies because this is likely to be powered 
# via USB-3 port
arm_freq=1000
gpu_freq=500

[none]

